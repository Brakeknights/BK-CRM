{
  "mcpServers": {
    "hostinger-mcp": {
      "type": "stdio",
      "command": "node",
      "args": ["./node_modules/.bin/hostinger-api-mcp"],
      "env": {
        "API_TOKEN": "${HOSTINGER_API_TOKEN}"
      }
    }
  }
}
